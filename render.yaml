services:
  - type: web
    name: contracthub-backend
    env: node
    region: oregon
    branch: main
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    plan: free
    autoDeploy: true
